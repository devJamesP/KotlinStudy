package chapter_05

/** Q1. 4번. 클래스는 다른 클래스가 상속할 수 있는 형태로 선언되어야 한다.
 * -> 상속할 수 있는 형태로 선언을 반드시 할 필요는 없다.
 * 상속 할 클래스에 open 키워드를 붙여주면 상속이 가능하다.
 *
 * Q2. 클래스의 멤버로 초기화 블록, (프로퍼티, 메서드), 중첩 클래스나 이너클래스가 포함될 수 있습니다.
 *
 * Q3. 주 생성자 선언시
 * class Customer public @Inject constructor(name: String) { ... }
*/
